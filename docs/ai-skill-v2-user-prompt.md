Generate one canonical Cube Skill v2 JSON definition for the following requested behavior. If it cannot be expressed within the catalog-backed, bounded contract, return a concise rejection explaining the missing capability.

Requested behavior:

%s
